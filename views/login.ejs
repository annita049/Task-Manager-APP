<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Oswald&family=Poppins:ital,wght@0,200;0,300;0,400;0,500;0,600;0,700;0,800;1,700&display=swap" rel="stylesheet">

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <!-- <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script> -->
    <title>Login Form</title>
    <style>
        *{
            font-family: "Poppins", sans-serif;
        }
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #edf2f7;
        }
        .login-form {
            width: 450px;
            padding: 40px 40px;
            border-radius: 5px;
            background-color: white;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }
        .input-group {
            position: relative;
            z-index: 1 !important;
        }
        .toggle-password {
            position: absolute; 
            top: 10%;
            /* display: none; */
            right: 10px; 
            background: none;
            border: none;
            cursor: pointer;
            z-index: 10 !important;
        } 
        
        .login-button {
            font-size: 1.1rem;
            padding: 9px 0;
            background-color: #4635b7;
            border-radius: 20px;
        }
        .login-button:hover {
            background-color: #2005cf;
            /* border-radius: 20px; */
        }
        input {
            border-radius: 20px !important;
            border: 1px solid #aaa !important;
            padding: 8px 18px !important;
        }
        .login-footer {
            padding: 0 10px;
            text-decoration: none;
            font-size: 16px;
        }
        .login-footer a{
            text-decoration: none;
        }
        .login-footer a:hover {
            text-decoration: underline;
        }
        .alerht {
            padding: 10px 20px !important;
            text-align: center;
        }
        .btn-close {
            outline: none;
        }
    </style>
</head>
<body>
<div class="login-form">

    <!-- Conditional rendering of Bootstrap alert -->
    <% if (typeof error !== 'undefined') { %>
        <div class="alert alert-danger alert-dismissible" role="alert">
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            <div><%= error %></div>
        </div>
    <% } %>

    <h2 class="text-center mb-4">Login</h2>
    <form action="/Login" method="POST">
        <div class="mb-3">
            <label for="email" class="form-label">Email address</label>
            <input type="email" class="form-control" id="email" name="email" placeholder="Enter your email" required>
        </div>
        <div class="mb-3">
            <label for="password" class="form-label">Password</label>
            <div class="input-group">
                <input type="password" class="form-control" id="password" name="password" placeholder="Enter your password" required>
                
                <button type="button" class="toggle-password fs-5" id="togglePassword">
                    <i class="bi bi-eye" id="eyeIcon"></i>
                </button>
            </div>
        </div>
        <button type="submit" class="btn btn-primary login-button w-100 mt-4">Login</button>
    </form>
    <div class="login-footer text-center justify-content-center mt-4 transition-text d-flex">
        <a href="/Register" class="register me-5">Register</a>
        <a href="#" class="password-reset">Forgot Password?</a>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<!-- <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.js"></script> -->
<script src="JS/login.js"></script>
</body>
</html>
